import Dates

mutable struct TimeManager
    final_time::Float64
    dt::Float64
    start::Dates.DateTime
    
    TimeManager(final_time,
                dt ) = new(final_time,
                           dt,
                           Dates.now() )
    
    
end
